   the trick is going to be: 
   component is the background and the dashed circle

   hovering background fires dashed circle
   But how to get the overlapping firing effect?
   elements at point mx my but is that too expensive to just do constantly? add a timeout so that it polls every .5 sec or something
   add to whole svg a mouseenter that starts the listen, mouseleave that turns all off and stops the listen

   add a hover to the hi too?


    fuction setSpinners(e)
    // elements at at pointer position 
    // filter to ones that have dataset.key
    // for each of those remaining crawlers[key].classList.add

    let activeCrawlers = [false, false, false]

    //setup mouseover crawl on circles
    let crawlerTriggers = Array.from(document.querySelectorAll('.crawlTrigger'));
    let crawlers = Array.from(document.querySelectorAll('.crawler'));

    function updateCrawlers(x,y) {
      const elements = document.elementsFromPoint(x,y).filter(ele => ele.dataset.key);
      activeCrawlers.forEach(c => )

    }

    crawlerTriggers.forEach((trigger, i) => {
      trigger.addEventListener("mouseover", (e) => {console.log(e); console.log(crawlers[i]); crawlers[i].classList.add('crawling-line')});
      trigger.addEventListener("mouseleave", (e) => {console.log(e); crawlers[i].classList.remove('crawling-line')});
    });